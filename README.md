tagla
-----

```js
new Tagla({
  data: [
    [x1, y1], [x2, y2]
  ],
  editor: true,
  formTemplate: '<form>...</form>' // need to provide x1, y1, x2, y2, width, height, tag columns
}).render();
```

## Config Options

* data
* editor
* formTemplate

## Method

* addInput
* addTag
